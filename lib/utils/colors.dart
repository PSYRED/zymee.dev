import 'package:flutter/material.dart';
class ItemColors {
  /*
  * I am creating an onSchedule color class as Colors have ran into a problem.
  * Using the imported package known as googleapis it seems that it is in conflict with
  * material UI package as both packages require and use the same color packages.
  * */

  Color btnColor = Colors.indigoAccent;

  Color txtColorYT = Colors.white;

  Color txtColorBL = Colors.black;

  Color background = Colors.white;




}